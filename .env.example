# EXAMPLE
#
# Note: some urls switch from localhost to 
# named docker services in prod/staging
# 
# e.g. prod: http://ai-img-validator:8000
#         dev: http://localhost:3000

# --- System-Profiler ---
# Dev: http://localhost:8787 (service runs in Docker, exposed to host)
# Prod: http://system-profiler:8787 (Docker service name)
SYSTEM_PROFILER_BASE_URL=http://localhost:8787

# --- Database ---
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydatabase

# Next.js runtime DB
DATABASE_URL=postgres://myuser:mypassword@localhost:5432/mydatabase

# Drizzle CLI / Studio
DATABASE_URL_EXTERNAL=postgres://myuser:mypassword@localhost:5432/mydatabase


# --- Auth ---
NEXTAUTH_URL=http://localhost:3000
AUTH_SECRET= # Added by `npx auth`. Read more: https://cli.authjs.dev


# --- OAuth ---
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=


# --- Image handling ---
# server-to-server (Next -> validator)
# Dev: http://localhost:8000
# Prod: http://ai-img-validator:8000 (Docker network)
PUBLIC_IMG_VALIDATOR_BASE_URL=http://localhost:8000

# server-only filesystem path (must match docker-compose volume mount)
# Dev: ./.data/uploads/images (local filesystem)
# Prod: /data/uploads/images (Docker volume mount path)
IMG_UPLOAD_DIR=/data/uploads/images


# --- Demo ---
SECRET_KEY=example-secret
NEXT_PUBLIC_SAFE_KEY=example-public
