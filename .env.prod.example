# Production Environment Template
#
# Copy this file to .env.prod and fill in your values:
#   cp .env.prod.example .env.prod
#
# Then sync to your production server:
#   rsync -avz .env.prod pi:~/pi-site/.env.prod

# --- Database ---
POSTGRES_USER=myuser
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD
POSTGRES_DB=mydatabase

# Next.js runtime DB (uses Docker service name 'db')
DATABASE_URL=postgres://myuser:CHANGE_ME_SECURE_PASSWORD@db:5432/mydatabase

# --- Auth ---
# Use your production domain
NEXTAUTH_URL=https://pi.rioredwards.com
NEXT_PUBLIC_SITE_URL=https://pi.rioredwards.com
AUTH_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# --- OAuth ---
# Create production OAuth apps pointing to your domain
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# --- Image handling ---
# Docker service name (internal network)
PUBLIC_IMG_VALIDATOR_BASE_URL=http://ai-img-validator:8000

# Container path (matches docker-compose volume mount)
IMG_UPLOAD_DIR=/data/uploads/images

# --- System Profiler ---
# Docker service name (internal network)
SYSTEM_PROFILER_BASE_URL=http://system-profiler:8787
# Auth token for protected endpoints (generate with: openssl rand -base64 32)
SYSTEM_PROFILER_AUTH_TOKEN=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# --- App Keys ---
SECRET_KEY=CHANGE_ME_GENERATE_SECURE_KEY
NEXT_PUBLIC_SAFE_KEY=CHANGE_ME_GENERATE_PUBLIC_KEY

# --- Umami Analytics ---
# Secret for Umami sessions (generate with: openssl rand -base64 32)
UMAMI_APP_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32
# Get these from Umami dashboard after first login (admin/umami)
NEXT_PUBLIC_UMAMI_WEBSITE_ID=your_website_id_from_umami_dashboard
# URL where Umami is accessible (for the tracking script)
NEXT_PUBLIC_UMAMI_URL=https://yourdomain.com/umami

# --- Optional: Admin Users ---
# Comma-separated list of provider-accountId values
# ADMIN_USER_IDS=github-123456,google-789012
